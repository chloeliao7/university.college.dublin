public class OperatorTutorial {
	public static void main(String[] args) {
		// int a = 7;
		// int b = 19;
		// int c = 7;


		}
		/* 		
				if(true  | true)  pass will check both
				if(true  | false) pass will check both
				if(false | true)  pass will check both
				if(false | false) no pass will check both
				if(true  || true)  pass doesnt check the second 
				if(true  || false) pass doestn check the second one 
				if(false || true)  pass if false it checks the second 
				if(false || false) no pass

				--------

				if(true & true)  pass will still check the second one 
				if(true  & false) pass will still chcek the second one 
				if(false & true)  pass will not check the second one 
				if(false & false) no pass wont check 
				if(true  &&  true)  pass checks the second one but needs to be the same 
				if(true  &&  false) pass checks the second one but needs to be the same 
				if(false &&  true)  pass doesnt check the second one but needs to be the same 
				if(false &&  false) fuck it 
		 */
	}
}
